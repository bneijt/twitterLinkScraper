Simple script to scrape urls from the Twitter sample stream.

Start
=====
- Move to the root directory (next to `bootstrap.sh`)
- Run `./bootstrap.sh`
- Place your app credentials in `credentials.py`


